<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="MauiApp1.MainPage">

    <ScrollView>
        <VerticalStackLayout
            Padding="30,0"
            Spacing="25">
            <Entry x:Name="YourText"
                   Placeholder="twoj tekst"/>

            <Button x:Name="Save" Clicked="SaveBtnClicked" Text="Save"/>
            <Button x:Name="Read" Clicked="ReadBtnClicked" Text="Read"/>

            <Label x:Name="Result" Text=""/>
        </VerticalStackLayout>
    </ScrollView>

</ContentPage>
